# Сессии: кто за что отвечает

С 23.09.2026 работа идёт в нескольких сессиях Claude, у каждой своя роль и свои
папки. Этот файл ведёт координатор (правило 1, раздел 0 `START_HERE.md`).

Аналогия: кухня ресторана. У каждого повара свой участок, шеф на раздаче
раздаёт заказы и один ведёт общую доску, контролёр пробует готовое блюдо, не
зная, как его готовили. На чужой участок повара не заходят — иначе двое солят
один суп.

| Роль | Пишет только сюда | Сейчас |
|---|---|---|
| Координатор | `START_HERE.md`, `HANDOFF.md`, `tennis/README.md`, этот файл, `docs/SESSION_PROMPTS.md`, остальные `docs/` кроме двух ниже, доска | раздаёт задачи, ведёт статусы |
| Запись | `deploy/`, `tennis/ingest/` кроме `onewin/`, `tennis/tests/` кроме `test_onewin_client.py`, `docs/TRACK_A_betboom_capture.md` | служба 1win на VPS в ветке (`563d5e1`, `b3961ba`) — ждёт ревьюера; дальше рекордер на ноутбуке, архив |
| Модель | `tennis/markov/`, `tennis/ratings/`, `tennis/model/`, будущие `tennis/state/` `features/` `replay/` `eval/` | калибровка (шаг 5) сдана — ждёт ревьюера |
| Рынок и латентность | `tennis/market/`, `tennis/ingest/onewin/`, `tennis/tests/test_onewin_client.py`, `docs/MARKET_MARGINS_2026-09-22.md` | 1win и первое сравнение двух контор приняты 24.09; пуши против снимков — исправление ждёт ревьюера; дальше счётчики рекордера 1win |
| Ревьюер Записи | ничего: только читает и запускает проверки | блоки «Записи» |
| Ревьюер Модели | ничего: только читает и запускает проверки | блоки «Модели» |
| Ревьюер Рынка | ничего: только читает и запускает проверки | блоки «Рынка» |

Нужна правка в чужих папках — не делать самому, а сказать владельцу: он
передаст роли-хозяину. `research/` не трогает никто, это журнал.
`requirements.txt` и `conftest.py` — через координатора.

## Общие правила для всех ролей

1. **Каждый свой ответ начинай строкой с ролью**, например `[Ревьюер]`:
   владелец переключается между вкладками и должен сразу видеть, где он.
2. Начало: `git pull`, затем `START_HERE.md` (раздел 0 обязательно),
   `HANDOFF.md` и свой раздел здесь.
3. **Прежде чем что-либо делать — опиши владельцу план**: что именно сделаешь,
   по шагам, подробно, к каждому шагу простая аналогия из жизни. Жди его «да».
   Так — перед каждой новой задачей, не только перед первой. Пиши
   по-человечески: пункт доски или раздел называй словами, а не только номером,
   без внутренних терминов; после каждого шага — строка «что сделал» с аналогией.
   **С 24.09 план пишется в режиме планирования** — для рабочих ролей и
   координатора: `git pull`, потом режим планирования (инструмент
   `EnterPlanMode`). В нём вкладка только читает, а выходит из него только с
   одобрением владельца — это и есть «да». Первая строка плана — что нового в
   ветке по твоей теме (`git log`) и не сделал ли это уже кто-то: 24.09 «Рынок»
   спланировал развёртывание 1win, которое «Запись» к тому времени уже сделала.
   Ревьюеров это не касается: они ничего не меняют, их заказ — сам блок.
4. Пишешь только в свои папки. Общие документы и доску не трогаешь: свой итог
   пишешь в сообщение коммита и в README своего модуля.
5. Коммит — только своих путей: `git add <свои пути>`, **никогда `git add -A`**.
   Локальные сессии делят одну папку, и `-A` забирает чужую незаконченную
   работу. Если git просит имя:
   `git -c user.name=Claude -c user.email=noreply@anthropic.com commit ...`.
6. Перед пушем `git pull --rebase --autostash`. Пуш только в
   `claude/tennis-prediction-audit-kskn26`, в `main` — никогда. Конфликт в
   чужом файле — стоп, сказать владельцу. Фоновое скачивание в цикле (слежение
   за отчётом и т. п.) — только `git fetch --no-write-fetch-head`: обычный
   `fetch` переписывает общий `.git/FETCH_HEAD`, и у соседей `git pull --rebase`
   падает с «Cannot rebase onto multiple branches» (так было 23.09).
7. Готово — это `python -m pytest tennis/ -q` → `0 failed`. Числа сверять с
   выводом команд, не с документами.
8. В конце работы — **блок для ревьюера**. Владелец отнесёт его ревьюеру твоей
   роли, а после вердикта «принять» — координатору. **Вердикта не ждать:**
   выдал блок — берёшь следующую задачу; пришло «ИСПРАВИТЬ» — возвращаешься к
   нему первым делом.

```
НА РЕВЬЮ — <роль>
Коммиты: <хеш> [<хеш> ...]
Что сделано: 2–5 строк
Как проверить: команды
Заявленные числа: число — какой командой получено
Что не проверено: ...
```

## Координатор

Аналогия: диспетчер. Водители не рисуют маршрутную карту сами — они
докладывают, а карту ведёт один.

- Раздаёт задачи ролям: что дальше — по `HANDOFF.md` и доске.
- После вердикта ревьюера «принять» переносит статус в `START_HERE.md`,
  `HANDOFF.md`, `tennis/README.md` и на доску. Без вердикта статус не меняет.
- Доска — приватный артефакт https://claude.ai/artifact/GUxjgFDRg8KYcoRHD4kV43.
  Сначала прочитать целиком (Artifact, действие read), править точечно,
  публиковать поверх той же ссылки.
- Код не пишет.
- Раздел «Сегодня» на доске переделан под роли 23.09. Напоминать владельцу:
  VPS оплачен до 06.10.2026. Из решений главы 7 доски ждёт одно — бюджет на
  данные (7.2): спросить, когда встанет вопрос, около 29.09. Сегмент и цель
  этапа решены 23.09.

## Запись

Аналогия: оператор самописца. Следит, чтобы лента шла и на ней было то, что
нужно, а не пустая линия.

- VPS `multiple-plum`, служба `betboom-capture`, пишет круглосуточно. Отчёт
  каждые 5 минут — ветка `capture-status`, файл
  `status/multiple-plum.ptr.network.json` (`git fetch origin capture-status`).
  На сервер заходит владелец: команды ему — одной строкой, без символов
  разметки, с проверкой, что он на сервере. Перезапуск службы и `git pull` на
  сервере — только с его «да».
- Срочные поломки на VPS (по тревоге сторожа) владелец чинит в отдельной
  сессии Claude Code, а её правку вносит «Запись». Так 23.09 разобраны обрывы:
  после 15:20 МСК их делает сам фид (ноутбук ловил их в те же пятиминутки);
  правка `5f3d1ea` и `5735617` принята ревьюером и с 17:52 23.09 стоит на
  сервере. Замер первого обрыва на новом коде принят (`14dcd04`): 46.3 с без
  цен — фид пять раз подряд не пускал обратно. Мелочь на следующую правку: в
  `deploy/README.md:185` написать «Если `blind_s` за час вырос больше чем на
  минуту…» — сейчас «оно» читается как `last_blind_s`, который не копится.
- Потом — рекордер на ноутбуке (PID 21472, запущен 22.09 17:39). Он на старом
  коде, без починки слотов, и за 23.09 записал 377 КБ против 47 МБ на VPS.
  Предложить владельцу перезапустить его на новом коде или остановить; самому
  не трогать.
- В ветке, ждёт «Ревьюера Записи»: служба 1win на VPS рядом с BetBoom
  (`563d5e1`, шаг 7 `deploy/README.md`) и отчёт сервера, который судит BetBoom
  только по его файлам (`b3961ba`). На сервер — только после «ПРИНЯТЬ»: там
  `git pull` подтянет и новый отчёт, который читает почасовой сторож.
- Потом — ночной архив на Google Диск владельца (5 ТБ, решено 23.09):
  закрытые дневные файлы всех контор каждую ночь уходят через rclone, с сервера
  удаляется только проверенное. Задача целиком — `docs/SESSION_PROMPTS.md`.
- Потом — пункт 3.7: сверить `APP_BUILD` в `runtime-env.js` конторы со схемой в
  `tennis/ingest/betboom/proto/`. И 4.6: чтобы ноутбук не засыпал, если он
  остаётся запасным.
- Осторожно: рекордер импортирует `tennis.market.names`, а на VPS лежит
  разреженный клон. Что всё нужное туда попадает, сторожит
  `tennis/tests/test_sparse_checkout.py`.

## Модель

Аналогия: инженер прибора. Собирает сам прибор, который по ходу матча
уточняет оценку подачи, как врач уточняет диагноз с каждым новым анализом.

- Шаг 4, живое состояние v1, принят ревьюером 23.09 (`221b19e`, `f61bda2`,
  `0b1b4bd`): Beta на очко подачи у каждого игрока, n0 подобран на истории,
  прирост к приору +0.0036…+0.0068 log loss (`HANDOFF.md`, «Трек B»). Мелочь на
  следующую правку: устаревший комментарий `GAME_ROWS_N0 = 40` в
  `tennis/eval/live_state.py:38`.
- Сейчас: шаг 5 — остаточная модель и калибровка на out-of-time, в новой
  вкладке (прежняя закрыта на половине контекста); полный текст задачи —
  `docs/SESSION_PROMPTS.md`. Потом шаг 6 — replay строго по `ts_received`.
- Известное ограничение: данные рейтингов кончаются турнирами 1 июня 2026.
- Сегмент решён владельцем 23.09: мужчины, ATP-тур + Challenger; женщины и ITF
  не входят.
- `tennis/` ничего не импортирует из `research/`.

## Рынок и латентность

Аналогия: хронометрист у двух табло курсов. Смотрит, какое табло меняется
первым, и всегда ли одно и то же.

- Принято ревьюером 24.09: сборщик 1win и первое сравнение двух контор
  (`03dbc59`, `781a817`, `78ae788`) — 15 минут на ноутбуке, BetBoom первым в
  82 % сопоставленных движений (92 % решённых), 1win позже на медиану 2.1 с,
  лидер тот же в 12 матчах из 13. Это первое чтение, не ответ.
- У «Ревьюера Рынка»: пуши против снимков (`c743343`, `f8901ce`, `3eb0f93`) —
  откатов нет, пуш опережает снимок на полсекунды. По `c743343` и `f8901ce` был
  вердикт ИСПРАВИТЬ; исправление `3eb0f93` ждёт вердикта.
- Дальше: счётчики рекордера 1win, чтобы отчёт сервера отличал запись от
  холостого хода (саму службу 1win на VPS уже сделала «Запись», ждёт её
  ревьюера); очерёдность на сутках записи; расхождение двух контор в оценке p
  (число «1.6 п.п.» в START_HERE ничем не вычислено). Полный текст задач —
  `docs/SESSION_PROMPTS.md`. Обе записи — на одной машине; меритель проверяет
  это сам.
- Осторожно: `tennis/market/__init__.py` и `names.py` импортирует рекордер на
  VPS. Не добавлять туда импортов вне `market/` и `markov/`.

## Ревьюеры

Аналогия: ОТК на заводе. Видит только готовую деталь и чертёж, не знает, как
её делали, — и поэтому ничего не прощает. С вечера 23.09 контролёров три, по
одному на линию: «Ревьюер Записи», «Ревьюер Модели», «Ревьюер Рынка», — чтобы
проверки шли параллельно и рабочие вкладки не простаивали.

- Каждый берёт блоки «НА РЕВЬЮ» только своей роли; блок чужой роли — сказать
  владельцу, что он не туда.
- Код не пишет, ничего не коммитит и не пушит. Только читает, запускает тесты и
  проверочные команды.
- Тесты гоняет все, не только своей роли: на стыках модулей (код «Рынка»,
  который импортирует рекордер на VPS; строки на гейм, которые берут n0 из
  живого состояния) ошибка видна только в чужих тестах.
- На вход — блок «НА РЕВЬЮ» от владельца. На него сначала 2–3 строки: что и как
  проверишь, с аналогией; отдельного «да» не нужно — сам блок и есть заказ.
- На каждый коммит — **новый агент** `python-reviewer` (для не-Python —
  `code-reviewer`, если таких нет — `general-purpose`) с заданием: найти
  реальные дефекты в диффе, проверить заявленные числа командами, отделить
  подтверждённое от подозреваемого. Новый — потому что сама сессия от ревью к
  ревью копит контекст, а агент каждый раз чистый.
- Каждую находку агента перепроверить самому, воспроизведением. Прогнать
  `python -m pytest tennis/ -q`.
- Проверить рамки: коммит пишет только в папки своей роли и не трогает общие
  документы.
- Ответ владельцу — вердикт: **ПРИНЯТЬ** или **ИСПРАВИТЬ** со списком: файл и
  строка, сценарий, что сломается, как чинить. Коротко.

## Промпт для новой сессии

**Полные стартовые промпты всех ролей — `docs/SESSION_PROMPTS.md`**: с
контекстом, фактами, числами, границами и критериями «готово». Вставлять их, а
не короткую форму ниже. Короткая форма — только скелет для новой роли, которую
координатор распишет так же подробно. Первая строка — роль: по ней владелец
узнаёт вкладку.

```
<РОЛЬ> — <чем занимается, одной строкой>
Репозиторий geogrgik-debug/mily, ветка claude/tennis-prediction-audit-kskn26.
Сделай git pull, прочитай START_HERE.md, HANDOFF.md и docs/SESSIONS.md:
общие правила и раздел своей роли.
Каждый свой ответ начинай строкой [<Роль>].
Прежде чем что-либо делать, опиши мне план: что именно сделаешь, по шагам,
подробно, к каждому шагу простая аналогия из жизни. Жди моего «да».
```
