# START HERE

Входная точка проекта. Если ты открыл репозиторий и не помнишь ничего — читай
этот файл целиком, он самодостаточен. Глубокое состояние и план лежат в
[`HANDOFF.md`](HANDOFF.md), числа — в
[`docs/EXPERIMENT_B_elo_prior_and_process.md`](docs/EXPERIMENT_B_elo_prior_and_process.md).

**Обновлено:** 24 сентября 2026 • **Ветка разработки:** `claude/tennis-prediction-audit-kskn26`

> **Обзор на одной странице для чтения глазами:** [дашборд «Следующий гейм»](https://claude.ai/artifact/GUxjgFDRg8KYcoRHD4kV43)
> — девять глав с пунктами и статусами, кто что делает сегодня, два графика,
> словарь с аналогиями; пересобран 22.09 вечером. Он **не файл репозитория**,
> а приватный артефакт, поэтому ссылка записана здесь: иначе сессия, которая
> видит только checkout, о нём не узнает. Доску, как этот файл и `HANDOFF.md`,
> правит только сессия-координатор — см. раздел 0.

---

## 0. Параллельные сессии: кто что правит

В ветку одновременно пишут несколько сессий, и 23.09 они начали мешать друг
другу: все правили одни и те же строки этого файла и `HANDOFF.md` — прежде всего
счётчики тестов и статусы. Итог: лишний ребейз, а готовый сборщик строк на гейм
(`4b38ee6`) в документах и на доске ещё полдня значился «следующим». Отсюда три
правила.

1. **Общие документы пишет одна сессия — координатор**, та, где владелец
   раскладывает задачи. Общие — это `START_HERE.md`, `HANDOFF.md`,
   `tennis/README.md`, `docs/SESSIONS.md` и доска. Рабочая сессия их не правит:
   свой итог она пишет в сообщение коммита и в README своего модуля, а
   координатор переносит статус. **Роли, их папки и промпт для новой сессии —
   `docs/SESSIONS.md`.**
2. **Счётчиков тестов в документах нет.** Критерий один — `0 failed` в выводе
   pytest. Число тестов меняется с каждым коммитом, и каждая его правка была
   конфликтом.
3. **Коммит — только своих путей, перед пушем — `git pull --rebase --autostash`.**
   Локальные сессии делят одну папку, поэтому `git add -A` забирает чужую
   незаконченную работу. Конфликт в файле, который правил не ты, не разрешать
   за другого: остановиться и сказать владельцу.

Аналогия: общая таблица в офисе. Пишет в неё один человек, остальные присылают
ему свои цифры — иначе каждый затирает чужое, и никто не знает, какая версия
верная.

---

## 1. Что это за проект

Живая система, которая для теннисного матча в реальном времени оценивает
вероятность того, что подающий удержит **следующий, ещё не начавшийся** гейм, а
позже — распределение точного счёта этого гейма (8 исходов: 40-0, 40-15, 40-30,
с ровно, и то же со стороны брейка).

**Чем это НЕ является.** Это не машина для денег. Формулировка задачи с самого
начала: исследование плюс paper trading. Причина не в осторожности, а в
измерении — см. пункт 2: на рынке точного счёта требуемое преимущество уже
посчитано и оно больше нашей собственной точности. Любой документ или человек,
который обещает здесь стабильный доход, противоречит числам в этом репозитории.

**Текущая фаза.** Исследование закрыто, идёт стройка. Всё, что можно было
посчитать на исторических данных, посчитано; всё остальное узнаётся только
записью живых данных.

---

## 2. Числа, которые надо знать раньше, чем что-то менять

Все получены на реальных данных, не из литературы. Log loss — на прогноз
удержания следующего гейма, мужская сетка Grand Slam, тест 2019–2024,
66 697 сервисных геймов.

| Что | Сколько | Смысл |
|---|---|---|
| Elo-приор вместо грубого | **+0.0123** | главный и почти единственный рычаг |
| Накопленная статистика подачи в матче | +0.0030 | второй по величине |
| Сторона принимающего | +0.0005 | есть, но мало |
| Исходы предыдущего гейма | +0.0001 | **ноль**, интервал через ноль |
| Процессные признаки Hawk-Eye (скорость подачи, длина розыгрыша, пробег) | **−0.0001** | **ноль.** Это закрыло всю видео-ветку |
| Ошибка приора после вычета биномиального шума | 5.5 п.п. на туре, 6.4 на Challenger | по вероятности очка |
| То же в вероятности удержания гейма | 10 и 13 п.п. | множитель dP(hold)/dp = 1.92 при p = 0.62 |
| Остаток ошибки после 40 очков внутри матча | 4.5 п.п. | live-статистика уменьшает, но не убирает |
| Маржа: точный счёт гейма | **20.0 % BetBoom**, 25.0 % Пари | поток; июльский скриншот давал 19.9 % — сошлось |
| Маржа: победитель следующего гейма (2 исхода) | **11.82 %** | 171 наблюдение; WTA 11.89 %, WTT/ITF 7.99 % |
| Какой гейм котирует контора | **следующий, 74 % случаев** | ровно то, что мы моделируем |
| Точность самой конторы | **~1–2 п.п.** | два её рынка на одном гейме расходятся на 1.09 |
| Требуемое преимущество на точном счёте гейма | **7.1–10.0 п.п. по вероятности очка** | по измеренной марже 20 %; раньше стояло 5.1 — устарело |
| Расхождение двух контор в оценке p | 1.6 п.п. — **нигде не вычислено**; первая прикидка BetBoom–1win: 1.5 (0.9–1.9) на 13 геймах WTT, с пропорциональным снятием маржи 0.7 | против нашей ошибки 5.5; число ждёт суток записи |
| Лимиты контор | не ограничение: потолок **выплаты** 1.5 млн ₽ BetBoom, 3.5 млн ₽ Пари | не потолок ставки |

**Два вывода, на которых держится всё остальное.**

Первый: на точном счёте гейма требуемое преимущество (7.1–10.0 п.п. по
измеренной марже 20 %) **превышает** нашу полную неопределённость (5.5). Рынок
закрыт количественно. На двухисходном «победителе следующего гейма» требуется
4.2–6.0 на туре и **2.8–4.0 на WTT/ITF** — и только там требование ниже нашей
ошибки. Это единственная незакрытая ставочная гипотеза.

Второй, и он неприятнее: две независимые конторы расходятся в оценке p на
1.6 пункта, а два рынка **одной** конторы на одном гейме — на 1.1 пункта. То
есть её точность порядка одного-двух пунктов, а наша 5.5: она точнее нас в
три-пять раз. Любая схема, которая начинается со слов «наша модель увидит
раньше рынка», обязана сначала объяснить это расхождение. Честная формулировка
оставшейся гипотезы не «мы обгоним рынок», а «на нижних уровнях контора может
деградировать быстрее нас» — и это проверяется только записью.

---

## 3. Что закрыто и не строится

Не возвращаться к этому без нового измерения. Каждый пункт закрыт расчётом или
экспериментом, обоснования — в аудите.

- **Видео и computer vision.** Процессные признаки Hawk-Eye дали ноль у обоих
  полов, в подвыборке с трекингом и отдельно без приора. Если самые чистые
  процессные данные, какие существуют, не дают ничего, то распознавание позы с
  трансляции тем более.
- **Мультиагентные дебаты (MAD), агенты-спорщики.** Нечего обсуждать: вся
  информация сводится к одному числу p.
- **RAG по соцсетям и новостям.**
- **Отдельная модель точного счёта.** Все 8 исходов — функция одного p.
  Проверено четырьмя независимыми сверками с реальными книгами, максимальное
  расхождение 0.014.
- **Машина состояний NORMAL → HOT → DOMINANT.** Заменена непрерывным
  апостериором. Фиксированный порог «расхождение 5 очков» срабатывает на
  68–78 % геймов из одного шума.
- **Momentum как отдельный признак.** Движение цены после проигранного очка —
  чистая марковская механика, а не «рынок что-то узнал». Это был центральный
  пример исходного блюпринта, и он опровергнут расчётом.

---

## 4. GitHub и ветка

| | |
|---|---|
| Репозиторий | `geogrgik-debug/mily` → https://github.com/geogrgik-debug/mily |
| Ветка разработки | `claude/tennis-prediction-audit-kskn26` |
| Pull request | не открывался; вся работа идёт коммитами в ветку |

Ветка одна, и она общая: в неё пушили несколько сессий параллельно. Поэтому
**перед началом работы всегда `git pull`**, иначе получишь отказ на пуш и будешь
разводить конфликты руками (так уже было).

```bash
git clone https://github.com/geogrgik-debug/mily.git
cd mily
git checkout claude/tennis-prediction-audit-kskn26
git pull
```

Если клонировать не даёт — репозиторий приватный, нужен вход в GitHub. В VS Code
это Account в левом нижнем углу → Sign in with GitHub. Либо через CLI:
`gh auth login`.

Правило пуша, которое действовало всю работу: **только в эту ветку**, в `main`
не пушить.

```bash
git add <свои пути>          # не -A: папку делят несколько сессий (раздел 0)
git commit -m "что сделано и почему"
git pull --rebase --autostash
git push -u origin claude/tennis-prediction-audit-kskn26
```

---

## 5. Локальная установка

Python 3.11 или новее.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m pytest tennis/ -q
```

**В PowerShell активацию лучше не делать вообще.** `Activate.ps1` падает с
`PSSecurityException` на машине, где не разрешён запуск скриптов, а это состояние
по умолчанию. Вместо этого зови интерпретатор venv напрямую — он тот же самый:

```powershell
cd путь\к\mily            # именно в корень репозитория, иначе не найдётся requirements.txt
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
.venv\Scripts\python.exe -m pytest tennis/ -q
```

Команды вводить **по одной**. Вставленный блоком многострочник PowerShell
склеивает через `>>` и выполняет не так, как ожидаешь — на этом уже потеряли
один заход.

Ожидаемый результат: **0 failed**. Пока не собраны классы protobuf, часть
тестов пропускается — тесты рекордера BetBoom и декодера его кадров в
`tennis/market/tests/test_lead_lag.py`; это нормально. Числа тестов здесь
намеренно нет (раздел 0): оно растёт с каждым коммитом, истина — вывод pytest.
Классы собираются локально и лежат в `.gitignore`:

```bash
mkdir -p tennis/ingest/betboom/generated
python -m grpc_tools.protoc -I tennis/ingest/betboom/proto \
    --python_out=tennis/ingest/betboom/generated \
    tennis/ingest/betboom/proto/bb_sport_ws_v1.proto
python -m pytest tennis/ -q
```

В PowerShell то же одной строкой — перенос через `\` там не работает, а `mkdir`
и так создаёт промежуточные каталоги:

```powershell
mkdir tennis/ingest/betboom/generated
python -m grpc_tools.protoc -I tennis/ingest/betboom/proto --python_out=tennis/ingest/betboom/generated tennis/ingest/betboom/proto/bb_sport_ws_v1.proto
python -m pytest tennis/ -q
```

После этого пропусков нет: **0 failed, 0 skipped**.

Многострочные команды в остальных документах написаны для bash. В PowerShell
переносить их надо либо в одну строку, либо через обратную кавычку `` ` `` вместо
`\`. На этом уже спотыкались.

### Про VS Code конкретно

- **Интерпретатор.** Ctrl+Shift+P → «Python: Select Interpreter» → путь
  `.venv\Scripts\python.exe` внутри репозитория (не системный Python и не venv,
  случайно созданный уровнем выше). Без этого тесты в панели Testing не найдутся.
- **`import tennis` работает из корня** благодаря `conftest.py`, который кладёт
  корень репозитория в `sys.path`. Запускать pytest надо из корня, не из
  подкаталога.
- **Панель Testing** настроена заранее: в репозитории лежит
  `.vscode/settings.json`, где pytest включён и нацелен на `tennis`. Если тесты
  всё равно не видны — выбери интерпретатор и нажми Refresh Tests.
- Русский текст в документах — UTF-8, никаких настроек не требует.

---

## 6. Карта репозитория

```
START_HERE.md          этот файл
HANDOFF.md             состояние, план по пунктам, что осталось открытым
requirements.txt       зависимости, разбитые по группам
conftest.py            кладёт корень в sys.path, чтобы работал import tennis

docs/
  SESSIONS.md                             роли сессий, их папки, промпт для новой сессии
  AUDIT_Tennis_Live_Next_Game_v1.md       аудит концепции, 26 разделов, ~186 КБ
  EXPERIMENT_B_elo_prior_and_process.md   эксперименты с числами, errata, 3 ревью
  TRACK_A_betboom_capture.md              как записывать линию BetBoom
  MARKET_MARGINS_2026-09-22.md            маржа по рынкам, «следующий гейм»,
                                          требуемое преимущество
  Tennis_Next_Game_Audit_v1.pdf           аудит для чтения с телефона (1-я редакция)
  Tennis_Audit_Bundle_for_review.md       самодостаточный пакет для внешнего ревью

tennis/                ПРОДАКШН-код. Импортируется без побочных эффектов, покрыт тестами
  markov/              ГОТОВО. Вероятностное ядро
    core.py              p_game, p_hold_from, exact_score_dist, p_tiebreak,
                         p_set, p_match, invert
    tests/               тесты ядра
  ingest/              ГОТОВО. Коллектор (трек A)
    clock.py             парные показания настенных и монотонных часов
    ids.py               ULID (сортируемые по времени) и отпечатки полезной нагрузки
    names.py             идентичность матча между провайдерами
    rawlog.py            append-only лог сырых кадров, .jsonl.gz (4.8× измерено)
    betboom/
      extract_schema.py  восстанавливает protobuf-схему из JS-бандла конторы
      proto/             восстановленная схема: 223 сообщения, 1012 полей, 25 enum
      client.py          сам рекордер
      generated/         вывод protoc, НЕ в git, собирается локально
    onewin/              ГОТОВО. Рекордер 1win: живые цены с его push-сервера
                         (socket.io), каждый кадр — в лог до разбора
  market/              ГОТОВО. Чтение доски букмекера
    names.py             market_name -> (сет, гейм, вид рынка)
    overround.py         маржа, пропорциональная нормировка, Shin
    fit.py               одно p, стоящее за восемью исходами гейма
    measure.py           CLI: маржа и качество подгонки по записанному логу
    streams.py           сырой лог BetBoom и 1win -> события цены; проверка «одна
                         машина, одни часы»
    join.py              какой матч одной конторы — какой у другой, по фамилиям игроков
    p_gap.py             CLI: насколько две конторы расходятся в оценке p, гейм за геймом
    lead_lag.py          CLI: кто из контор двигает цену первым, на сколько, устойчиво ли
  ratings/             ГОТОВО. Приор подачи: Elo + Barnett–Clarke, снимок на диске
    snapshot.py          RatingsSnapshot: build / save / load / prior / add_result
    elo.py serve.py      Elo и окна подачи/приёма, перенесены из research/elo_prior.py
    __main__.py          python -m tennis.ratings download | build | evaluate | prior
  model/               ШАГИ 3 И 5 ГОТОВЫ. Строки для модели и калиброванная P(hold)
    game_rows.py         строка на сервисный гейм из того, что было известно до
                         него; утечка исключена построением, тест отравляет будущее
    hold.py              P(удержит следующий гейм): живое состояние + поправка на
                         контекст гейма + калибровка; p_hold — для живого процесса
    hold_v1.json         подобранные параметры, пишет python -m tennis.eval calibrate
  state/               ШАГ 4 ГОТОВ (v1). Живое состояние подачи
    beta.py              Beta на очко подачи у каждого игрока, центр — приор,
                         сила n0 подобрана на истории
  features/            ГОТОВО. Контекст гейма для поправки: счёт, уровень, покрытие
  eval/                замер на истории: Grand Slam PBP и tennis_pointbypoint,
                         python -m tennis.eval download | live-state | calibrate
  replay/              ещё не создан

research/              ЛАБОРАТОРНЫЙ ЖУРНАЛ. Скрипты, прогнанные один раз
  elo_prior.py           Elo + марковская модель + инверсия (источник ядра)
  calc.py                НЕ ИМПОРТИРУЕТСЯ: гонит симуляции на уровне модуля
  slam_process_study.py  датасет сервисных геймов из Grand Slam PBP
  mcp_next_game_study.py исследование на Match Charting Project
  elo_sweep.py  confound_test.py  leak_check.py  b2_robustness.py
  *_results.json         выводы прогонов от 21.09.2026
  betboom_july_bets.txt  137 одиночных ставок, июль 2026
```

**Граница между `tennis/` и `research/` жёсткая:** `tennis/` ничего не
импортирует из `research/`. `research/` не трогать и не рефакторить — это запись
того, что было сделано, с прибитыми результатами.

### Что читать в аудите, а что нет

Раздел 3 (где концепция ошибается), 12 (рынок), 26 (что строить первым),
приложение C (расчёты). **Раздел 10 про видео устарел** — ветка закрыта.
**Раздел 5 про литературу — числа непроверенные**, брались из статей, а не
пересчитывались.

---

## 7. Где мы в плане

### Трек A: коллектор. Стоит ноль, стартует первым

1. **Рекордер BetBoom** — написан. Линия идёт только по WebSocket в protobuf,
   подписка на матч отдаёт полный снимок его рынков на каждую смену счёта, а
   цены гейм-исходов поштучно — по `stakes_subscribe` (пункт 4). **Тот же
   сокет отдаёт счёт и сторону подающего**, то есть
   котировка и состояние приходят парой из одного источника с одной метткой
   времени. Схема восстановлена из JS-бандла, не подключаясь к серверу.
2. **Append-only лог** — написан.
3. **Живые сессии проведены** — 22.09.2026, две. Первая (с машины владельца)
   нашла два дефекта протокола: дерево ленивое, а `settings_set` сервер
   отвергает. Вторая (из песочницы агента, после починки) закрыла все
   неизвестные: 10 одновременных подписок держит; гейм-рынки есть везде, но
   **точный счёт гейма — только от Challenger и WTA 125 и выше**, на WTT/ITF
   лишь двухисходный «Исход»; рукопожатия сверх подписки не требуется.
4. **Движение цены внутри гейма пишется** — с 22.09.2026. Полный снимок идёт
   на смену счёта, а не по таймеру, поэтому переоценка во время розыгрыша в
   него не попадала: измерено опережение потока `newsletters_stake` — медиана
   0.5 с, p90 78 с, максимум 153 с, и одно изменение снимок не показал вовсе.
   Рекордер теперь шлёт `stakes_subscribe` (203 исхода, 1015 пушей за 5 минут
   на боевом прогоне). Подробности — в `docs/TRACK_A_betboom_capture.md`.
5. **Платный PBP-фид** — уже не для сшивки, а только для измерения абсолютной
   латентности: кто раньше видит очко, корт или контора.
6. **Измерить абсолютную латентность** на 20–50 матчах. **Это число решает,
   существует ли проект как live-система.** Ждёт решения по бюджету, которое
   сам план откладывает до конца первой недели записи.
   Относительную — какая контора двигает цену первой — меряет
   `python -m tennis.market.lead_lag`, бесплатно; нулевой тест на живой записи
   прошёл. Второй поток, 1win, пишется с 23.09 (`tennis/ingest/onewin/`).
   Первое чтение двух контор, 15 минут на ноутбуке: BetBoom двигает цену
   первым в 82 % сопоставленных движений, 1win позже на медиану 2.1 с (p90
   4.1 с), лидер тот же в 12 матчах из 13. Это первое чтение, не ответ:
   дальше — 1win на VPS рядом с BetBoom и сутки записи. Подробности — `HANDOFF.md`.

### Трек B: движок. Параллельно

1. **Модуль `markov/`** — ГОТОВО.
2. **Оффлайн-джоб рейтингов** — ГОТОВО 22.09, `tennis/ratings/`. `build_elo` и
   `rolling_priors` из `research/elo_prior.py` со снимком состояния: сборка 10 с,
   файл 620 КБ, загрузка 0.12 с вместо перемалывания 270 тысяч матчей.
   Конфигурация из свипа: K = 400, экспонента 0.4, вес покрытия 0.3, смесь 0.6
   на Elo. Elo побитово равен исходнику, числа B1 воспроизведены до 4-го знака;
   на честной (прошлогодней) базовой линии эта конфигурация даёт RMSE смеси
   **0.0850** (B1 — 0.0855). Подробности и расхождения — `tennis/ratings/README.md`.
   **Данные кончаются турнирами 1 июня 2026**: для сегодняшнего матча рейтинги
   старше на 113 дней, а источника результатов после июня пока нет.
3. **Snapshot builder + тест на утечку** — ГОТОВО 23.09, `tennis/model/game_rows.py`
   (`4b38ee6`): строка на каждый сервисный гейм из того, что было известно до
   него. Утечка исключена построением — гейм попадает в накопители только после
   того, как его строка выдана, — а тест всё равно отравляет будущее и требует,
   чтобы прежние строки не изменились.
4. **Live-состояние** — ГОТОВО 23.09, v1 (`tennis/state/`; `221b19e`,
   `f61bda2`, `0b1b4bd`): у каждого игрока Beta на вероятность выиграть очко на
   своей подаче, центр — приор, сила — n0 псевдо-очков, подобранная на истории
   (Шлемы 140, тур 100, Challenger 90). Величин две, а не четыре: исход очка
   видит только два контраста «подача против приёма». Прирост к одному приору на
   отложенных годах, log loss удержания, 95 % интервал по матчам: Шлемы
   2019–2024 +0.0036 (0.0029–0.0043), тур 2017 +0.0043 (0.0034–0.0051),
   Challenger 2017 +0.0068 (0.0061–0.0077). Отклонения двух подающих от
   приоров коррелируют на −0.55…−0.60 — довод за совместное состояние в
   версии 2. Подробности — `tennis/state/README.md`.
5. **Остаточная модель + калибровка** — ГОТОВО 24.09 (`ef4b574`, `6dea990`;
   принято ревьюером): logit P(hold) = logit(живое состояние) + поправка на
   контекст гейма (15 признаков, одна логистическая регрессия на три уровня),
   поверх — beta-калибровка. Подгонка до 2017, замер с 2017 (Шлемы 2019–2024,
   тур и Challenger 2017). Прирост поправки к живому состоянию, log loss, 95 %
   по матчам: всё вместе +0.0013 (0.0010–0.0015), Шлемы +0.0014, тур +0.0008,
   Challenger +0.0014; больше всего даёт счёт в сете, +0.0007. Калибровка:
   наклон 1.009, ECE 0.0039 (пол шума 0.0027). Для живого процесса —
   `tennis.model.p_hold`, параметры в `tennis/model/hold_v1.json`; подробности —
   `tennis/model/README.md` и `tennis/eval/README.md`.
6. **Replay-бэктест** строго по `ts_received` — СЛЕДУЮЩЕЕ; нужны записанные
   данные с VPS.

---

## 8. Первое, что надо сделать

Строго по приоритету.

1. **Запустить запись всерьёз** — **сделано 22.09:** запись круглосуточно идёт
   на VPS (`multiple-plum`, служба `betboom-capture`), отчёт каждые 5 минут в
   ветке `capture-status`, подробности — `HANDOFF.md`, «Машина записи». Ниже —
   как это устроено и как запускать с ноутбука, если понадобится. Разведка
   закончена, рекордер работает, потолок подписок ≥ 10, лог сжимается.
   Каждый день без записи — безвозвратно потерянный день: гейм-рынок живёт
   около минуты и не архивируется никем.
   ```powershell
   .venv\Scripts\python.exe -m tennis.ingest.betboom.client --out data/raw --max-matches 10
   ```
   Оставить работать надолго. Занимает 0.3–0.9 ГБ в сутки, переживает обрывы
   связи (переподключение с нарастающей паузой), новый день пишет в новый файл.
   Остановить — Ctrl+C, он допишет концевик и напечатает отчёт.

   **Лучше всего — на отдельной Linux-машине:** `bash deploy/vps-setup.sh`
   ставит всё с нуля (проверено от чистого разреженного клона), а
   `deploy/betboom-capture.service` делает из этого службу, переживающую
   перезагрузку. Ноутбук — плохой дом: он спит, его закрывают, его увозят.

   Если всё же на ноутбуке — **в отдельном окне PowerShell,** не в фоне сессии
   агента и не во встроенном терминале редактора: и то и другое умрёт вместе с
   сессией. Записи
   не нужен ни ИИ, ни агент — это обычная программа, и стоит она ноль.
   Погашенный экран записи не мешает, **сон машины — останавливает**
   (`powercfg /change standby-timeout-ac 0` и «действие при закрытии крышки —
   не требуется»). Подробности в `docs/TRACK_A_betboom_capture.md`.

   Разовые проверки и разбор протокола можно делать из сессии агента — сокет
   через прокси проходит, вопреки тому, что было записано раньше.
2. **Сверить `APP_BUILD`** в `runtime-env.js` конторы перед каждым запуском.
   Номера полей protobuf — это контракт, и молчаливая перенумерация испортит
   запись, **не подняв ошибки**.
3. ~~**Калибровка**~~ — **готово 24.09** (трек B, пункт 5): калиброванная
   P(hold), `tennis/model/hold.py`. Дальше в треке B — прогон по записи
   (пункт 6); ему нужны записанные данные с VPS.

---

## 8a. Что уже работает по расписанию

Каждый час отдельная сессия сравнивает свежий отчёт VPS с отчётом часовой
давности — растут ли котировки и файл, есть ли подписки, не рвётся ли связь — и
будит владельца, если запись встала или пишет вхолостую. Когда всё хорошо,
молчит; раз в сутки, в 10:59 МСК, присылает сводку. Подробности и
id routine — в `HANDOFF.md`, раздел «Что крутится само».

---

## 9. Решения владельца проекта

1. **Сегмент — решено 23.09:** ATP тур + Challenger одной моделью с intercept
   по уровню, только мужчины. WTA требует своего рейтинга, которого нет, а её
   процессные признаки на чистой выборке тоже дали ноль. ITF, где требование к
   точности ниже нашей ошибки (пункт 2), в сегмент не входит.
2. **Бюджет на данные — ждёт.** Пересмотрено: запись котировок BetBoom стоит
   ноль, и трек A при нуле уже не отпадает. 150–300 $/мес нужны только для
   платного PBP-фида, то есть для измерения латентности. Владелец ответит, когда
   встанет вопрос: после первой недели записи, около 29.09.
3. **Цель первого этапа — решено 23.09:** калиброванная оффлайн-модель плюс
   работающий коллектор. Paper trading с симуляцией исполнения — третий месяц,
   не первый. **Обе части есть с 24.09:** калибровка принята ревьюером, запись
   идёт на VPS круглосуточно.
4. **Архив записи — решено 23.09:** закрытые дневные файлы каждую ночь уходят
   с VPS на Google Диск владельца (5 ТБ). Это задача «Записи»; до её приёмки
   данные забираются по-старому, шаг 6 `deploy/README.md`.

---

## 10. Грабли, на которые уже наступили

Это самая полезная секция файла. Каждый пункт — реальный баг или ошибка, которая
стоила времени и в двух случаях **переворачивала вывод**.

**Баги в коде, найденные и исправленные.**

- `invert()` возвращал baseline для каждой вероятности ниже 0.5 — половина всех
  значений была мусором. Бисекция искала только `d ≥ 0`, а самопроверка брала
  лишь 0.5, 0.6, 0.75 и 0.9 и ничего не заметила. **Это перевернуло главный
  вывод эксперимента B1.** Теперь прибито тестом round-trip от 0.05 до 0.95.
- Сетка (мужская/женская) определялась по тому, присоединился ли ATP-приор, а AO
  и RG за 2018–2021 сокращают имена. 762 мужских матча (26 679 геймов) попали в
  женскую выборку, а мужской тест потерял AO и RG целиком. Лечение: сетка из
  третьего сегмента `match_id`, имена нормализуются в «инициал + фамилия».
- Токен `GAME` в RG-2020, AO-2021 и RG-2021 читался как признак тайбрейка →
  ~1500 матчей выброшено молча.
- Sackmann ставит всем матчам турнира дату его начала, поэтому join приоров по
  (игрок, дата) раздул 406 920 строк до 3 088 401. Лечение: ключ по уникальному
  `match_key` и окна по строго более ранним турнирам.
- Битый `ElapsedTime` давал `sec_per_point` до 1e26. Признаки при этом
  **улучшали AUC и ухудшали log loss** — это подпись взрыва калибровки от
  выбросов, а не находка.
- Заполнение пропусков нулями смешивает «отклонения нет» и «не измерено».
  Лечение: винзоризация по перцентилям обучающей выборки плюс явные индикаторы
  пропуска.
- Baseline (`tour_spw` и средние по году/покрытию) считался по всей выборке,
  включая тест. Утечка. Лечение: baseline предыдущего года.
- Условие «без приора» в confound-тесте всё равно протаскивало приор через
  `live_spw_dev` (сжатие к `p_prior`, потом вычитание `p_prior`). Лечение:
  отдельный prior-free блок со сжатием к константе C = 0.62.
- `build_games` работал 700+ секунд процессорного времени на миллионах мелких
  вызовов pandas. Переписан одной векторной `groupby.agg` → 11 секунд.

**Ошибки в арифметике документов, исправленные.**

- Множитель чувствительности был написан 2.6 при собственной вычисленной
  производной 1.92 — все производные числа были завышены примерно на треть.
- Отношение шума Challenger к туру было заявлено «1.5×» при подстановке общего
  биномиального шума вместо поуровневого. Правильно 1.16.
- «14 конфигураций» на самом деле было 18.

**Ловушки идентификации.**

- Три исхода с одной стороны определяют p только с точностью до `p ↔ 1−p`:
  `h30/h15 = 2.5q` — зеркало `b30/b15 = 2.5p`. Первая попытка дала вероятности
  удержания 7–25 %, что невозможно. Лечение: ограничение `p ≥ 0.5`.

**Операционные.**

- `pkill -f elo_prior.py` дважды убивал собственную оболочку, потому что шаблон
  совпадал с текстом самой команды. Сканировать `/proc/*/cmdline`.
- Четыре оригинальных репозитория данных Sackmann удалены с GitHub в июне–июле
  2026. Живёт зеркало `Aneeshers/tennis-sackmann-archive`. Датасетов на диске
  нет, пока их не скачать: контейнеры пересоздаются. Скачиваются одной командой
  и из PowerShell тоже: `python -m tennis.ratings download data/sackmann/atp`
  (54 файла, 52 МБ). 22.09 скачаны заново, и `research/elo_prior.py` с
  `research/elo_sweep.py` дали на них побайтно те же `*_results.json`, что
  лежат в репозитории, — зеркало то самое. Grand Slam PBP всё ещё не скачан.
- Свип B1c (`research/elo_sweep.py`) брал базовую линию подачи **того же**
  года, в котором сам матч, — утечку, которую `elo_prior.py` к тому времени
  уже исправил на прошлый год. Поэтому в таблицах B1 и B1c у одного и того же
  Barnett–Clarke разные числа (0.0890 и 0.0888), а выбранная конфигурация на
  честной базовой линии даёт 0.0866 / 0.0850, а не 0.0865 / 0.0849. Выбор
  конфигурации и вес 0.6 от этого не меняются.
- Я «починил» в `rolling_priors` правило исключения незавершённых турниров под
  его docstring — и потерял матч в 1 532 окнах. Код был умнее описания: если
  игрок уже доиграл более поздний турнир, его матчи на раннем точно сыграны.
  Урок: прежде чем чинить журнал под его описание, измерить, что изменится.
- PowerShell подменяет `curl` на `Invoke-WebRequest`, поэтому bash-синтаксис с
  `\` и `| head -c` там не работает. Нужен `curl.exe` одной строкой.
- Фильтр вида спорта по подстроке «теннис» подписал рекордер на Setka Cup —
  настольный теннис, чьё дерево пришло первым и заняло все слоты. Совпадение
  слага должно быть точным (`tennis`, id 4).
- Репозиторий по умолчанию открывается на `main`, а там только README и фото.
  `git clone` без `--branch claude/tennis-prediction-audit-kskn26` даёт машину
  без `deploy/` и без рекордера — раннбук VPS так и был написан.
- Разреженный клон для VPS не включал `tennis/tests/`, и `vps-setup.sh`
  останавливался на шаге тестов с «no tests ran» (код 5), не дойдя до конца.
  Юнит systemd требует существующий `data/` (`ReadWritePaths`), а его никто не
  создавал. `heartbeat-push.sh` из мелкого клона (`--depth 1`) не видел
  `origin/capture-status`, а его worktree наследовал разреженность и отказывался
  добавлять `status/*.json`. Все четыре найдены 22.09 прогоном раннбука через
  настоящий разреженный клон и исправлены; «проверено от чистого клона» до
  этого означало полный клон, а не тот, что стоит на машине записи.
- Две ветки, каждая верная по отдельности, вместе дали нерабочую машину
  записи: раннбук проверили на разреженном клоне, когда рекордер импортировал
  только `tennis/ingest/`, а в параллельной ветке рекордер тем временем начал
  импортировать `tennis.market` (`3f695d8`). Проверка «слияние без конфликтов»
  такого не видит — конфликт был в смысле, а не в строках, и `git merge-tree`
  на свежей голове дал ещё и два обычных. Найдено 22.09 при слиянии тем же
  разреженным клоном; теперь это сторожит `tennis/tests/test_sparse_checkout.py`.
  Урок: ветка одна, а раннбук проверяется после слияния, не до.
- Первая установка на живой VPS споткнулась трижды, и ни разу — о код записи.
  Команду вставили в PowerShell на своём компьютере вместо окна, подключённого к
  серверу (`Лексема "&&"...`). По дороге через чат из неё пропали `__`, `**` и `$`
  — чат принял их за разметку. А на Ubuntu 26.04 `/tmp` — tmpfs на 1 ГБ, и 9
  тестов статуса, которые мерили свободное место во временной папке, упали
  («1.1 дня записи»). Лечение: `deploy/install-service.sh` и cone-форма
  разреженного клона без спецсимволов, тесты статуса больше не смотрят на диск
  хоста. Урок: команды для человека — без символов разметки и с явной проверкой
  «вы на сервере».
- `.venv` (6 443 файла, 29 МБ) был закоммичен в 474f62f, потому что его не было
  в `.gitignore`. Убран из индекса 22.09, в `.gitignore` добавлен.
- «Прокси песочницы не пропускает WebSocket» — было записано как измеренный
  факт и оказалось **неверным**: `101 Switching Protocols` проходит, кадры идут.
  Из-за этого разведка протокола ждала чужой машины, хотя могла идти сразу.
  Урок: «не работает» — это результат конкретной команды с конкретным выводом,
  а не вывод из документации прокси.
- Рекордер, который печатает только при ошибке соединения и раз в 200
  котировок, при нуле котировок молчит — и не отличим от зависшего. Любой
  долгоживущий процесс обязан иметь сердцебиение.
- Три типа ответов из 32 обрабатывались, остальные молча выбрасывались, в том
  числе `error` и `code ≠ 200` внутри типизированных ответов. Первая живая
  сессия из-за этого выглядела как «сокет пустой», хотя сервер в ответах
  называл и ошибку, и виноватое поле.
- `.venv\Scripts\Activate.ps1` падает с `PSSecurityException`, если запуск
  скриптов не разрешён — а это состояние Windows по умолчанию. Политику менять не
  нужно: `.venv\Scripts\python.exe` работает и без активации.
- Многострочные команды, вставленные в PowerShell блоком, склеиваются через `>>`
  и выполняются не по одной. Плюс отдельная классика: запуск из родительского
  каталога вместо корня репозитория даёт `Errno 2` на `requirements.txt` и venv
  не в том месте.
- Ночь 22–23.09 машина записи провела вхолостую. Подписки на матчи только
  копились: вечерние десять доиграли, а каждый следующий матч упирался в предел
  `--max-matches`. С 01:35 МСК котировок не было, хотя отчёт показывал «10
  подписок», а файл рос — на общем потоке счёта. Поймал ежечасный сторож в первую
  же ночь («пишет вхолостую»). Исправление `000106a`: слот освобождается, когда
  фид удаляет матч из live-дерева или помечает его завершённым, а страховкой —
  после 20 минут тишины; освободившийся слот берёт следующий живой матч. Урок: у
  ограниченного ресурса должен быть путь назад, и тест обязан прогнать смену
  поколений — вечерние матчи кончились, ночные начались.
- 23.09 примерно с 11:37 до 11:51 МСК фид принимал каждое соединение и через
  долю секунды закрывал его кодом 3010 «Access rejected» — с машины записи и из
  песочницы одинаково, значит на их стороне; потом перестал сам. Рекордер за эти
  четверть часа переподключился 561 раз, раз в 1.6 с: пауза перед новой попыткой
  сбрасывалась на каждом рукопожатии, а отказ приходил уже после него. Заодно
  вскрылось ещё три вещи. Каждая сессия оставляла работать своё сердцебиение и
  фоновые циклы. Чистое закрытие (1000/1001 — так выглядит перезапуск сервера)
  не считалось обрывом, и рекордер заходил обратно, помня старые подписки, — та
  же тихая поломка «подписан, котировок нет», что уже чинили. А отчёт говорил
  «не подписан, 304 переподключения» и ни слова о причине — её пришлось добывать
  запуском со второй машины. Исправление `f35626c`: пауза сбрасывается только
  после сессии, прожившей минуту; любое закрытие — обрыв; циклы сессии умирают
  вместе с ней; причина последнего обрыва — в отчёте, поле `last_disconnect`.
  Урок: рукопожатие — ещё не соединение, а счётчик без причины — полдиагноза.
- 23.09 после 15:20 МСК фид начал рвать сессии без кадра закрытия, иногда потом
  отказывая в рукопожатии: к 16:10 обрывов 11, к 17:20 — 17; запись каждый раз
  возвращалась сама. Чья вина, решило сравнение: ноутбук ловил обрывы в те же
  пятиминутки, что и VPS, — значит фид, а не машина и не её провайдер. Отчёт при
  этом говорил только, сколько было обрывов, а не сколько они стоили, и после
  обрыва здоровой сессии рекордер выжидал паузу, как после отказа. Исправление
  `5f3d1ea`: сессия, прожившая минуту, переподключается сразу (отказы — по-прежнему
  с паузой 1–2–4…60 с), а отчёт показывает `blind_s` и `last_blind_s` — сколько
  секунд не было котировок — и последние десять обрывов с причиной. Урок: чей
  обрыв, быстрее всего показывает вторая запись с другой машины.
- Два рекордера на одной машине слышат один и тот же кадр не одновременно.
  22.09 на ноутбуке, 2553 одинаковых по байтам кадра: |Δ| медиана 8 мс, p90
  86 мс, p99 0.92 с, максимум 1.38 с, и один процесс был первым в 64 % случаев.
  Поток один — значит, это наш собственный шум приёма, и он даже не симметричен.
  Меритель очерёдности без полосы «одновременно» в 1 с назначил бы лидером один
  из двух захватов одного потока. Там же: после сна ноутбука (8.8 ч) его
  настенные часы прыгнули на 6.6 с — поэтому лаг меряется по монотонным часам.
  Урок: нулевой тест на живых данных находит то, чего синтетика не знает.

**Две мои ошибки, которые стоит знать, потому что они про суждение, а не про код.**

- Я сказал, что история ставок за июль «почти ничего не даст». Неверно: из неё
  вышли 17 одновременных снимков рынка и самая полезная находка той сессии.
- Я написал в тесте по памяти, что 4-пунктовое преимущество на подаче даёт
  больше 75 % на матч. Пересчёт: 69.1 %. Тест теперь утверждает структурный факт
  (преимущество строго растёт по иерархии: 0.040 очко → 0.070 гейм → 0.130 сет →
  0.191 матч), а не запомненное число.

---

## 11. Честно про риски

**Правовое и аккаунты.** Автоматический сбор почти наверняка противоречит
пользовательскому соглашению конторы, а аккаунты, показывающие прибыль,
ограничивают. Это решение владельца проекта, а не техническое ограничение. Стоит
держать в голове, что **запись публично отображаемых котировок и реальная игра с
того же счёта — разные по величине риски**.

**Защита сайта.** Перед BetBoom стоит QRATOR, плюс антифрод Group-IB (`/api/fl`,
ротация токена `cfids` каждые несколько секунд). На самом фид-сокете они не
наблюдались, но закладываться на это нельзя. Домен конторы и зеркала ротируются,
поэтому в коллекторе он должен читаться из конфига, а не быть вшит.

**Машина записи — тот же VPS, через который владелец выходит в интернет по
VPN.** Заходить в BetBoom через этот VPN, пока идёт запись, значит связать
аккаунт со сбором котировок по IP.

**Безопасность.** В одном из загруженных в сессию файлов лежал сессионный токен
1win открытым текстом. Он больше не нужен ни для чего (их API публичный, хватает
партнёрского id), и ту сессию стоит перелогинить. Токены в репозиторий и в чат
не кладём.

**Критерий остановки проекта.** Если после 5 тысяч прогнозов с синхронными
котировками разница с рынком неотличима от нуля ни в одном сегменте — гипотеза
про опережение рынка отвергнута. Тогда ценность проекта в другом: калиброванная
модель состояния игрока и объяснения, без ставочной составляющей. Это
заранее принятое условие, а не поражение.

---

## 12. Как продолжить с Claude Code в VS Code

Состояние на 22.09.2026, вечер, после слияния веток: разведка протокола
закончена, рекордер работает и проверен вживую, пишет и цены внутри гейма,
лог сжимается, `market/diff.py` сравнивает соседние снимки, `ratings/` даёт
приор подачи из снимка, `model/game_rows.py` строит строки на гейм без утечки,
`market/lead_lag.py` меряет очерёдность контор; тесты зелёные.
**Следующее действие — включить запись, а не продолжать разведку.**

### Три команды, чтобы догнать репозиторий

Открой папку репозитория в VS Code, терминал (Ctrl+`), по одной строке:

```powershell
cd C:\Users\Пользователь\.claude\mily
git pull
.venv\Scripts\python.exe -m pytest tennis/ -q
```

Ждём **0 failed** (без собранного protobuf часть тестов пропущена — это
нормально, см. пункт 5). Если что-то красное — сначала чинить это,
запись подождёт.

### Промпт для Claude Code

```
Репозиторий geogrgik-debug/mily, ветка claude/tennis-prediction-audit-kskn26.
Сначала git pull, потом прочитай START_HERE.md и HANDOFF.md целиком.
Состояние: разведка закончена, рекордер проверен вживую и чинился по живой
записи, лог сжимается, market/ читает доску букмекера, ratings/ и
model/game_rows.py готовы, тесты зелёные.
Работаем над: <задача от владельца>. Общие документы (START_HERE, HANDOFF,
tennis/README.md, доску) не правь — раздел 0 START_HERE; итог — в коммит.
```

Задачи по приоритету:

1. ~~**VPS**~~ — **готово 22.09**, запись идёт. Порядок — `deploy/README.md`;
   раннбук прошёл целиком на живой машине, включая юнит systemd. VPS оплачен до
   06.10.2026 — продлить.
2. ~~**Разностный разбор снимков**~~ — **готово**, `tennis/market/diff.py`
   (`1a47abc`): какие цены сдвинулись между двумя снимками матча.
3. ~~**`tennis/ratings/`**~~ — **готово** 22.09.
4. ~~**Snapshot builder и тест на утечку**~~ — **готово** 23.09,
   `tennis/model/game_rows.py`.
5. ~~**Живое состояние**~~ — **готово** 23.09, `tennis/state/`.
6. ~~**Калибровка**~~ — **готово** 24.09, `tennis/model/hold.py`. Следующее в
   треке B — прогон по записи; ему нужны записанные данные с VPS.

### Если что-то пошло не так во время записи

```powershell
.venv\Scripts\python.exe -m tennis.ingest.betboom.inspect_log data/raw
```

Инспектор читает записанный лог (сжатый тоже) и говорит, чем именно ответил
сервер: какие запросы ушли, какими типами он ответил, не было ли `error` или
`code ≠ 200`, и что лежит внутри первого кадра каждого типа. Один раз это уже
спасло: сессия выглядела зависшей, а в логе сервер прямым текстом называл и
ошибку, и поле, которое отверг.

Если в отчёте машины (`capture-status`) `reconnects` растёт сотнями, а
`subscribed` — 0, первым делом прочитать там же `last_disconnect`: это текст, с
которым сервер закрыл последний сокет. `3010 ... Access rejected` — отказ на
стороне фида (23.09 так было четверть часа); на машине ничего не трогать,
рекордер ждёт до минуты между попытками и сам подпишется, когда пустят.
С `5f3d1ea` в отчёте есть ещё `blind_s` и `last_blind_s` — сколько секунд запись
была без котировок из-за обрывов — и последние десять обрывов с причиной (на
VPS — с 17:52 23.09).

**Что не надо просить заново.**

- Ещё одно ревью аудита. Их было три (Gemini, Fable, Astra), находки разобраны в
  документе с экспериментами. Четвёртое ревью того же материала бесполезно.
- Вернуть видео, CV, MAD или RAG. Закрыто измерениями, см. пункт 3.
- Пересказать или сократить аудит. Он уже есть в четырёх формах, включая PDF.
- Рефакторить `research/`. Это журнал, а не код.
