# Состояние проекта и план. Передача в новую сессию

**Обновлено:** 23 сентября 2026 • **Ветка:** `claude/tennis-prediction-audit-kskn26`

Состояние и план. Если контекста нет совсем — сначала
[`START_HERE.md`](START_HERE.md): там что за проект, все измеренные числа,
GitHub, установка, карта репозитория и список грабель. Этот файл — про то, где
мы стоим и что делаем дальше.

---

## Где мы находимся

Исследовательская фаза закрыта. Три вопроса, которые определяли объём работы, получили ответы на данных, а не на мнениях. Дальше считать нечего: всё оставшееся неизвестное можно узнать только сбором live-данных, а это уже стройка.

### Что измерено

| Вопрос | Ответ | Где |
|---|---|---|
| Сколько стоит приор | **+0.0123** log loss против грубого приора | EXPERIMENT_B, B2 |
| Сколько стоит live-статистика подачи | +0.0030 | то же |
| Сколько стоит сторона принимающего | +0.0005 | то же |
| Исходы предыдущего гейма | +0.0001, интервал через ноль | то же |
| Процессные признаки Hawk-Eye | **0**, у обоих полов, в подвыборке с трекингом, и без приора | то же |
| Ошибка приора после вычета биномиального шума | 5.5 п.п. тур, 6.4 п.п. Challenger | B1 |
| Маржа на точном счёте гейма | **20.0 %** (июльский скриншот давал 19.9 % — сошлось) | MARKET_MARGINS |
| Маржа на победителе гейма | **11.82 %**; WTA 11.89 %, WTT/ITF 7.99 % | то же |
| Какой гейм котирует контора | **следующий, в 74 % случаев** | то же |
| Точность самой конторы | ~1–2 п.п. (два её рынка расходятся на 1.09) | то же |
| Лимиты | не ограничение: потолок выплаты 1.5–3.5 млн ₽ | то же |
| Требуемое преимущество на точном счёте гейма | **7.1–10.0 п.п.** по измеренной марже 20 % (раньше стояло 5.1 — устарело) | MARKET_MARGINS |

**Главный вывод:** на точном счёте гейма требуемое преимущество (7.1–10.0 п.п.) превышает нашу полную неопределённость (5.5). Рынок закрыт количественно. На двухисходном «победителе гейма» требуется 4.2–6.0 на туре и 2.8–4.0 на WTT/ITF — единственный рынок, где требование ниже нашей ошибки. Но контора точнее нас в три-пять раз (её два рынка расходятся на 1.1 п.п., наша ошибка 5.5), так что честная гипотеза — не «обгоним рынок», а «на нижних уровнях он может деградировать быстрее нас». Раньше здесь стояло «5.1 превышает 5.5» — это было арифметически неверно и по числу устарело.

### Что закрыто и не строится

Видео и computer vision, MAD и мультиагентные дебаты, RAG соцсетей, отдельная модель точного счёта, машина состояний NORMAL→HOT→DOMINANT. Каждое закрыто измерением или расчётом, обоснования в аудите.

### Главный блокер

Ни одной записанной котировки на гейм-рынок. Задним числом их не получить нигде: Betfair Historical гейм-рынки не содержит, остальные не архивируют. Каждый день без коллектора — безвозвратно потерянный день.

**Как его снять — выяснено 22.09.2026,** см. `docs/TRACK_A_betboom_capture.md`. Линия BetBoom отдаётся только по WebSocket протоколом protobuf, подпиской на матч: полный снимок его рынков, дальше новый полный снимок на каждую смену счёта (медиана 13.5 с). Сообщения на каждую отдельную цену (`newsletters_stake`) есть, но только по отдельной подписке `stakes_subscribe`, которую рекордер шлёт с `3f695d8`; без неё движение цены внутри розыгрыша в запись не попадало — слепое пятно до 153 с. Схема протокола восстановлена из JS-бандла клиента и лежит в `tennis/ingest/betboom/proto/` (223 сообщения, компилируется protoc). Тысячи наблюдений — вопрос удержания сессии, а не частоты опроса.

Главное следствие: **тот же сокет отдаёт счёт и сторону подающего.** Котировка и состояние приходят парой, из одного источника, с одной меткой времени. Отдельный PBP-фид для сшивки не нужен — он нужен только чтобы измерить, кто быстрее.

#### Второй источник: разведка API 1win

После того как BetBoom закрыл блокер, 1win перестал быть критическим путём. Он остаётся полезен для одного: сравнить маржу и разойтись ли две конторы в оценке p — на июльских данных они расходились на 1.6 пункта против нашей собственной ошибки приора 5.5. Разведка доведена до половины.

| Факт | Статус |
|---|---|
| Реальный хост API — `api-gateway.top-parser.com`, сайт `one-vv2420.com` только зеркало | установлено |
| API публичный: хватает заголовка `x-external-partner-id`, сессионный токен не нужен | установлено, проверено и с машины владельца, и из песочницы |
| `tournaments/get?tournamentId=` отдаёт структуру турнира | работает, но `cache-control: max-age=600` — для live бесполезно |
| `matches/get?matchId=` отдаёт 453 байта: составы, слаги, `service: "LIVE"`, `updatedAt` в миллисекундах | работает, **коэффициентов нет** |
| `sports/get?sportId=` отдаёт 90 байт справочника | работает |
| `markets/get`, `odds/get`, `outcomes/get`, `bets/get`, `lines/get`, `matches/get-markets`, `matches/get-list`, `matches/get?…&withMarkets=true` | **все 404 либо без коэффициентов** |

**Вывод:** `top-parser` — шлюз контента, а не котировок. Цены идут другим каналом. Наиболее вероятный кандидат — два запроса типа `json` длительностью 11.36 с в devtools: это long-poll, поэтому фильтр Socket и оказался пустым. Это та же архитектура, что у BetBoom, только без протобуфа.

**Что нужно, чтобы двинуться:** в devtools на странице матча щёлкнуть строку типа `json` со временем порядка 10 секунд, скопировать её **Request URL** и **Response**. Полные заголовки присылать не надо — токен в них не нужен, а один раз он уже утёк в загруженный файл открытым текстом, так что сессию стоит перелогинить.

Зеркало `one-vv2420.com` ротируется, поэтому в коллекторе домен должен читаться из конфига, а не быть вшит. То же относится и к хосту BetBoom.

**Каркас сборщика — `tennis/ingest/onewin/`, 23.09.** Конфиг без умолчаний: `ONEWIN_API_BASE` — всё, что в Request URL стоит до `matches/get` (префикс пути на хосте в репозитории нигде не записан), и `ONEWIN_PARTNER_ID`. Три известных get-эндпоинта, каждый ответ ложится в сырой лог до разбора, `--probe` проверяет конфиг с той машины, где запущен. Канал коэффициентов — заглушка: `ODDS_TODO` в `client.py` перечисляет, что решит каждая часть F12-запроса (хост, параметр матча, курсор следующего опроса, время удержания, форма котировки). Писать 1win для сравнения с BetBoom нужно **на той же машине**, что и BetBoom: меритель очерёдности проверяет это по логам и логи с разных машин не сравнивает.

---

## План

### Трек A: коллектор. Стартует первым, не зависит ни от чего

Порядок изменён 22.09.2026: котировки идут первыми, платный PBP-фид перестал быть входным условием. Первый этап стоит ноль.

1. **Рекордер BetBoom.** Написан и **проверен вживую 22.09.2026** — дважды: с машины владельца (нашёл два дефекта протокола) и из песочницы агента (оба исправлены, полный цикл работает). Даёт котировки и счёт со стороной подающего из одного сокета. Живые факты: 10 одновременных полных подписок — 10/10 по 200; гейм-рынки есть на всех уровнях, точный счёт гейма — от Challenger и WTA 125 и выше, на WTT/ITF только двухисходный «Исход»; адрес гейма лежит в `market_name` («2-й сет 6-й гейм: Точный счёт»), `period_name` пуст; `provider_id` — идентификатор Sportradar. Цены приходят **полными снимками** `newsletters_full_match` на каждую смену счёта (медиана 13.5 с) и, с `3f695d8`, поштучно — `newsletters_stake` по гейм-исходам, на которые рекордер подписывается через `stakes_subscribe` (боевой прогон: 203 исхода, 1015 пушей за 5 минут); счёт и подающий по всем матчам тура идут в `newsletters_match` без полной подписки. Объём: **сжатие сделано и измерено на живой записи — 4.8× на диске**, то есть 0.3–0.9 ГБ в сутки на десять матчей вместо 2–4 ГБ. Полный разбор в `docs/TRACK_A_betboom_capture.md`. **Из песочницы работает** — утверждение про CONNECT-прокси было неверным; отдельная машина нужна только для многочасовой записи. Разбор сырого лога: `python -m tennis.ingest.betboom.inspect_log data/raw`.
2. **Append-only лог.** Написан: `tennis/ingest/rawlog.py`. Сырые кадры ложатся на диск раньше разбора, дозапись не усекает файл, оборванная строка не ломает чтение. **Пишет `.jsonl.gz`** — каждому `fsync` предшествует `Z_SYNC_FLUSH`, поэтому гарантия при падении та же, а объём в 4.8 раза меньше (измерено на живой записи). Оборванный gzip-член отдаёт всё, что уцелело. Искать файлы — через `find_logs`, не `rglob("*.jsonl")`. Parquet — форма для анализа, собирается из закрытых дневных файлов, а не путь записи.
3. **Развёртывание.** `deploy/` — раннбук от пустого VPS до службы и отчёта о себе. 22.09 прогнан через настоящий разреженный клон из песочницы; найдены и исправлены четыре дефекта: клон без `--branch` попадает в пустой `main`; в разреженном наборе не было `tennis/tests/`, и установка останавливалась на тестах; `data/` не создавался, а юнит требует его через `ReadWritePaths`; heartbeat из мелкого клона не находил `origin/capture-status` и не мог добавить файл в разреженный worktree. Пятый найден при слиянии веток тем же разреженным клоном: с `3f695d8` рекордер импортирует `tennis.market.names`, а с ним `market/` и `markov/`, которых в наборе не было, — рекордер не запускался, установка падала на тестах. Набор расширен, его полноту сторожит `tennis/tests/test_sparse_checkout.py`; `vps-setup.sh` на коммите слияния прошёл целиком. **22.09 вечером раннбук прошёл целиком на живой машине записи, включая юнит systemd** — см. «Машина записи» ниже. По дороге починены ещё две вещи: `deploy/install-service.sh` делает шаги 4–5 одной командой (команда, скопированная через чат, теряла `__`, `**` и `$`), а тесты статуса перестали мерить свободное место во временной папке — на Ubuntu 26.04 это tmpfs на 1 ГБ, и 9 тестов падали на самой машине записи.
4. **PBP-фид.** Уже не для сшивки, а только для измерения латентности. Триалы двух из трёх: Goalserve (~1200 $/год, есть Challenger и ITF), api-tennis.com (40–80 $/мес), livetennisapi (архив с 2023).
5. **Измерить латентность** на 20–50 матчах: наша задержка против официального фида и против перепрайсинга конторы. Перепрайсинг теперь наблюдаем напрямую — по флагам `is_active` и `is_bet_stop` видно, когда контора сняла рынок и когда вернула его с новой ценой. **Это число решает, существует ли проект как live-система.**
   **Разрез «контора против конторы» — меритель готов, 23.09:** `python -m tennis.market.lead_lag A=путь B=путь` даёт очерёдность (по движению и в сумме), отставание (медиана, p90) и устойчивость лидера (доля первых с 95 % интервалом и по матчам). Цена — вероятность после снятия маржи (Шин), движения одного исхода сопоставляются по порядку, в одну сторону, в окне 10 с; логи с разных машин не сравниваются — проверяется по самим логам. **Нулевой тест на живых данных:** два рекордера на ноутбуке 22.09, 36 минут общего времени, 3247 движений исхода матча — B−A медиана −3 мс, |лаг| p90 86 мс, 99.2 % одновременно, лидера нет; независимая сверка по одинаковым кадрам (sha256) дала те же −3 мс и 86 мс. Хвост того же шума — до 1.38 с, и один процесс был первым в 64 % кадров, поэтому «одновременно» — это внутри 1 с: быстрее этого очерёдность двух контор нашими средствами не различить. Лаг длиннее окна меритель не видит, а цепляет соседнее движение — поэтому отчёт перепроверяет каждую пару удвоенным окном (проверено сдвигом живой записи: 12 с при окне 10 с). Ждёт второй поток — 1win, см. «Второй источник» выше.

### Трек B: движок. Параллельно

1. ~~**Модуль `markov/`.**~~ **Готово.** `tennis/markov/` — ядро из `research/elo_prior.py` плюс `p_hold_from` и `exact_score_dist`, вынутые из неимпортируемого `research/calc.py`. 299 тестов, 2.4 с: тождества, round-trip инверсии от 0.05 до 0.95 (регрессионный щит для бага с аутсайдерами), точечное Монте-Карло 200 тыс. прогонов на гейм, тайбрейк и распределение восьми исходов, плюс побитовая эквивалентность исходнику. `python -m pytest tennis/ -q`.
2. ~~**Оффлайн-джоб рейтингов.**~~ **Готово 22.09.** `tennis/ratings/`: `build_elo` и `rolling_priors` из `research/elo_prior.py` со снимком состояния (`RatingsSnapshot`: сборка 10 с, 620 КБ, загрузка 0.12 с; `add_result` дописывает новые матчи той же арифметикой — снимок, достроенный по одному результату за все 7 194 матча 2026 года, равен полной пересборке). Конфигурация из свипа: K=400, экспонента 0.4, вес покрытия 0.3, смесь 0.6 на Elo. Сверка с исходником на настоящих данных: загрузка и порядок совпали на всех 270 662 матчах, Elo побитово в обеих конфигурациях, окна подачи — кроме 652 из 411 124 (ничьи по дате, которые исходник упорядочивал нестабильной сортировкой), все RMSE таблицы B1 — до 4-го знака. Боевая конфигурация на честной базовой линии: Elo **0.0866**, смесь **0.0850**, вес, подобранный на обучении, — 0.60; истинная ошибка `p` 5.5 п.п. на туре и 6.3 на Challenger (было 5.5 и 6.4). **Ограничение:** данные зеркала кончаются турнирами 1 июня 2026, живому процессу нужен источник результатов после июня — пока его нет. Всё — `tennis/ratings/README.md`.
3. ~~**Snapshot builder + leakage-тест.**~~ **Готово 23.09** (`4b38ee6`, облачная сессия): `tennis/model/game_rows.py`, строка на каждый сервисный гейм — цель (удержал ли подающий), приор, подача и удержания обоих игроков до этого гейма, прошлый сервисный гейм, контекст счёта. Утечка исключена построением: гейм попадает в накопители только после того, как его строка выдана; тест всё равно отравляет все геймы после k-го и требует, чтобы первые k строк не изменились. Приор — функция-параметр, поэтому без файлов данных в тестах и `RatingsSnapshot.prior(...)` вживую. 14 тестов.
4. **Live-состояние.** Байесовское обновление по очкам, начать с бета-биномиального. **Следующее.**
5. **Остаточная модель + калибровка.** Логит на контексте поверх структурной вероятности, beta-калибровка на out-of-time.
6. **Replay-бэктест** по `ts_received`.

### Структура репозитория

```
tennis/
  ingest/   ГОТОВО — рекордер BetBoom, append-only лог, часы, идентичность (трек A);
            onewin/ — каркас сборщика 1win, канал цен ждёт F12
  markov/   ГОТОВО — вероятностное ядро (трек B)
  ratings/  ГОТОВО — Elo и as-of приоры, снимок на диске (трек B)
  market/   ГОТОВО — чтение доски букмекера, маржа, diff снимков, очерёдность контор
  model/    шаг 3 ГОТОВ — строки на гейм без утечки (трек B)
  state/   features/   replay/   eval/   ещё не созданы
research/   остаётся как лабораторный журнал
conftest.py кладёт корень репозитория в sys.path, чтобы `import tennis` работал
```

`python -m pytest tennis/ -q` → **0 failed.** Пока не собраны классы protobuf (они в `.gitignore`, рецепт в `tennis/ingest/betboom/README.md`), тесты рекордера BetBoom и декодера его кадров пропускаются; после сборки пропусков нет. Числа тестов здесь намеренно нет — раздел 0 `START_HERE.md`: оно растёт с каждым коммитом, и его правка каждой сессией была конфликтом.

### Точки решения

- **Через 2 недели.** Если наш фид систематически позже перепрайсинга конторы — live-исполнение невозможно, проект становится чисто исследовательским.
- **Через 6–8 недель.** Первое сравнение с рынком на записанных данных.
- **Критерий остановки.** Если после 5 тысяч прогнозов с синхронными котировками разница с рынком неотличима от нуля ни в одном сегменте — гипотеза отвергнута.

---

## Что крутится само, без сессии

**Routine «Проверка записи BetBoom (ежечасно)»**, id `trig_01KZG924NGgJccJgn27NxiRN`,
cron `59 * * * *` UTC — раз в час по просьбе владельца с вечера 22.09 (до этого раз
в два часа); каждый запуск — свежая сессия. Клонирует историю ветки
`capture-status` за сутки и сравнивает отчёт каждой машины с отчётом часовой
давности: мало того, что машина жива, — пишет ли она полезное.

- **Тревога, пуш сразу:** отчёт старше 20 минут; `ok: false`; подписан, а
  котировки за час не выросли (пишет вхолостую); час подряд без подписок (один
  ноль в тихий час — не тревога); файл того же запуска не растёт; диска меньше
  чем на 3 дня.
- **Предупреждение:** диска меньше чем на 7 дней (пора забрать данные); за час
  3 и больше обрывов связи; рекордер перезапускался.
- **Всё в порядке — молчит.** Раз в сутки, в 07:59 UTC (10:59 МСК), — сводка:
  часы без тревог, котировки и мегабайты за сутки, перезапуски, обрывы, диск.
  Со 2 по 6 октября в каждом сообщении напоминает продлить VPS.

Задержка обнаружения — до ~1 ч 20 мин; сама машина считает себя нездоровой уже
через 5 мин без записи и через 10 мин без котировок (`tennis/ingest/status.py`).
Логика проверена 22.09 на настоящих отчётах: за час +156 959 котировок и
+11.7 МБ, вердикт «молчать». Стоимость: прежняя проверка шла 43 с на Sonnet,
~$0.16; раз в час это порядка $110–150 в месяц, вдвое больше, чем раз в два часа.
Остановить или сделать реже: `update_trigger` (enabled=false или другой cron) или
из интерфейса Routines.

Машина записи отчитывается сама: `deploy/heartbeat-push.sh` по cron каждые 5
минут кладёт `status/<hostname>.json` в ветку `capture-status`. С 22.09 вечером
там отчитывается машина записи `multiple-plum` (ниже); до неё ветка была пуста
намеренно (временный статус из песочницы удалён, чтобы не будить ложной тревогой
после смерти контейнера). Скрипт 22.09 проверен из
такого клона, какой стоит на машине записи (`--depth 1 --sparse`), против копии
удалённого — и с существующей веткой, и без неё; до этого из такого клона он не
работал, см. грабли в `START_HERE.md`.

---

### Машина записи

**`multiple-plum`**, VPS владельца: Ubuntu 26.04, Python 3.14.4, 1 ядро, 2 ГБ, 30 ГБ
NVMe, судя по тарифу — Нидерланды. Пишет круглосуточно с вечера 22.09: служба
`betboom-capture` из `/root/capture` (разреженный клон в cone-режиме, root), отчёт
каждые 5 минут в `status/multiple-plum.ptr.network.json`. Первый отчёт — `ok`,
10 подписок, 263 398 котировок за первый час с небольшим, ноль переподключений,
свободно 25 ГБ = 27.9 дня по верхней оценке. Проверить, не заходя на сервер:
`git fetch origin capture-status` и прочитать этот файл.

**23.09 оба исправления стоят на машине с 12:40 МСК.** Ночью с 01:35 МСК запись
шла вхолостую — слоты подписок не освобождались (`000106a`). В 11:37–11:51 фид
отказывал всем сессиям кодом 3010, и рекордер долбил его переподключениями раз в
1.6 с (`f35626c`). Оба случая — в граблях `START_HERE.md`. В 11:51 запись вернулась
сама (переподключение стёрло старые подписки), а в 12:40 владелец поставил
исправления: `git pull` 438b22f → df7bef8 и перезапуск службы, запуск
`run-1790156413`. Первый отчёт нового кода, 12:45: `ok`, 10 подписок, 1 991
котировка за первые пять минут, ноль переподключений, поле `last_disconnect` на
месте. Обновлять машину и дальше так же, одной строкой на сервере:
`cd ~/capture && git pull && systemctl restart betboom-capture`.

**После 15:20 23.09 фид рвёт сессии** без кадра закрытия: к 16:10 обрывов 11,
к 17:20 — 17, запись каждый раз возвращалась сама. Вина фида: ноутбук ловил
обрывы в те же пятиминутки. Исправление `5f3d1ea` (и `5735617` — строка в
граблях `deploy/README.md`) принято ревьюером 23.09: сессия, прожившая минуту,
переподключается сразу, а в отчёте появляются `blind_s`, `last_blind_s` и
последние десять обрывов с причиной. На машине — после обновления той же одной
строкой; до него отчёт идёт от запуска `run-1790156413` и этих полей не
содержит. «~3 с до котировок» в `deploy/README.md` взято из лога облачной
сессии и не проверено — подтвердит `last_blind_s` с VPS.

- **Оплачен до 06.10.2026.** Не продлить — запись встанет, а данные на сервере
  могут пропасть вместе с ним.
- **Диск 30 ГБ, а не 60.** Забирать данные раз в одну-две недели и удалять
  закрытые дневные файлы — шаг 6 `deploy/README.md`. Решено заменить это ночным
  архивом на Google Диск — «Решения владельца проекта», пункт 4.
- **На той же машине стоит VPN владельца.** Пока идёт запись, в BetBoom через этот
  VPN не заходить: иначе аккаунт связывается со сбором котировок по IP.
- Запись на ноутбуке больше не единственная; её можно остановить или оставить
  запасной.

## Решения владельца проекта

1. **Сегмент — решено 23.09:** ATP тур + Challenger одной моделью с intercept по уровню, только мужчины. WTA требует своего рейтинга, которого нет; ITF в сегмент не входит.
2. **Бюджет на данные — ждёт.** Пересмотрено 22.09.2026: запись котировок BetBoom стоит ноль, и трек A при нуле уже не отпадает. 150–300 $/мес нужны только для платного PBP-фида, то есть для измерения латентности. Владелец ответит, когда встанет вопрос: после первой недели записи, около 29.09.
3. **Цель первого этапа — решено 23.09:** калиброванная оффлайн-модель плюс работающий коллектор. Paper trading — третий месяц.
4. **Архив записи — решено 23.09:** закрытые дневные файлы каждую ночь уходят с VPS на Google Диск владельца (5 ТБ) через rclone; с сервера удаляется только проверенное. Задача «Записи»; до её приёмки — по-старому, шаг 6 `deploy/README.md`.

---

## Что осталось открытым в исследовании

Не блокирует стройку, но стоит закрыть попутно.

- Исследование на Match Charting Project не перегнано с нормальным приором: там до сих пор грубый приор и заполнение нулями.
- Восемь проверок устойчивости не перегнаны на исправленной выборке.
- Неоднородность конструкции признаков между обучением и тестом (доля геймов с короткой историей падает с 41 % до 14 %).
- Индикаторы пропусков кодируют корт, а значит посев.
- Эффект Meier на наших данных не проверен.
- Рейтинга WTA нет.
- Скачан, но не использован `tennis_pointbypoint` (57 690 матчей ATP/Challenger/ITF) — закрывает главную дыру обобщения за пределы Grand Slam.
- ~~Не измерена маржа двухисходного рынка «победитель следующего гейма».~~ **Измерено 22.09.2026**, см. `docs/MARKET_MARGINS_2026-09-22.md`: медиана **11.82 %**, а не 9.9 % с июльского скриншота (то наблюдение лежало у нижнего края). WTA 11.89 %, WTT/ITF **7.99 %**. Попутно: контора котирует **следующий** гейм в 74 % случаев — ровно то, что мы моделируем; восемь исходов действительно сводятся к одному p (108 живых книг, медианный остаток 0.01); два рынка одной книги расходятся в P(hold) на 1.09 п.п.
- Датасеты Sackmann на диске не живут — контейнеры пересоздаются. Скачиваются одной командой: `python -m tennis.ratings download data/sackmann/atp` (зеркало `Aneeshers/tennis-sackmann-archive`, оригинальные репозитории удалены в июне–июле 2026). 22.09 скачаны заново: `research/elo_prior.py` и `research/elo_sweep.py` дали на них побайтно те же `*_results.json`, RMSE 0.0871/0.0855 подтверждены. Grand Slam PBP заново не скачивался.

### Сверка чисел между документами, 22.09.2026, вторая сессия

Проверено чтением всех документов, `research/*_results.json` и прогоном pytest.
Выводов это не меняет, но каждое либо починено, либо должно быть на виду.

- **Провенанс «5.1».** `MARKET_MARGINS` §5 писал, что прежние 5.1 п.п. получены «на марже 20 % и чувствительности 1.92». На деле 5.1 — это **половина книги** при марже 25 % и `dP/dp = 2.45` (`EXPERIMENT_B`, «Что это меняет»); полная книга там же — **10.2**, и она согласуется с новыми 7.1–10.0. Строка исправлена; вывод прежний.
- **«4.2–6.0 на туре»** выведено из маржи **WTA** (11.89 %, n = 128). Маржа двухисходного рынка на ATP и Challenger **не измерена** — в таблице по уровням только WTA и WTT/ITF. Число для мужского тура пока заимствовано у женского.
- **1.6 п.п. между двумя конторами** нигде в репозитории не вычисляется: в `EXPERIMENT_B` BetBoom и Пари сравниваются по марже и потолкам выплат на **разных** матчах. Измерено только расхождение двух рынков одной книги — 1.09 п.п. Оценка «контора точнее нас в 3–5 раз» держится на 1.09 (пятикратно) и на неподтверждённых 1.6 (трёхкратно). И то и другое — согласованность конторы с самой собой, а не её ошибка против исхода; ошибку против исхода даст только запись.
- **Ошибка приора внутри `EXPERIMENT_B`** приводится двумя наборами: 0.0554 тур / 0.0641 Challenger (B1, после вычета шума) и 0.0541 тур / 0.0643 вся выборка / 0.0442 после 40 очков (таблица B1b, в тексте рядом — 0.0449). Расхождения в 0.1 п.п. вывод не меняют, но «6.4» в одном месте — Challenger, в другом — вся выборка.
- **Объём записи.** `TRACK_A` даёт для одной и той же минуты WTA два сырых размера: 1 539 641 байт в тексте и 2 130 КБ в таблице (38 %); отсюда «2.2 ГБ в сутки» против 3.0. Сжатие: 4.8× (WTT, живая запись на диск) и 5.4× (WTA, gzip одним проходом) — оба измерены, документы называют первое, код называл второе. Лог пропал вместе с контейнером, не перепроверить. Диск на два месяца по верхней границе — 54 ГБ, не 40.
- **`EXPERIMENT_B`: «278 273 гейма»** не сходится ни с собственной разбивкой (159 078 + 95 123 = 254 201), ни с `research/slam_process_results.json` (`n_games` = 254 201).
- **Счётчики.** «405 прошло, 1 пропущен» без protobuf — на деле 377 / 2 (исправлено). «10–27 ГБ в месяц» — 9–27 (исправлено). «10 и 13 п.п.» по гейму — 6.4 × 1.92 = 12.3.

### Сверка чисел, 22.09.2026, третья сессия — после перезапуска research на скачанных данных

- **B1c против B1.** `research/elo_sweep.py` брал базовую линию подачи того же года, что и матч (утечка), `elo_prior.py` — прошлого. Поэтому у одинакового Barnett–Clarke в двух таблицах 0.0888 и 0.0890, а у конфигурации K=250/0.5 смесь 0.0854 при весе 0.60 против 0.0855 при 0.55. Выбранная K=400/0.4/0.3 на честной базовой линии: Elo 0.0866, смесь 0.0850 (в таблице B1c — 0.0865 и 0.0849), вес 0.60 подтверждается. Выводы не меняются.
- **Колонка «В пересчёте на P(hold)» в таблице B1** не следует методу из текста. Текст: множитель 1.92 при p = 0.62; по нему 0.0554 → 10.6 п.п. и 0.0641 → 12.3 п.п. В таблице 9.6 и 12.7 — так выходит, если брать свой p на уровень (≈ 0.64 тур, ≈ 0.61 Challenger). «Около 10–13 п.п.» верно при любом из двух способов, но текст и таблица расходятся.
- **406 920 строк в B1 — на 18 больше, чем есть.** В файлах четыре матча, где у победителя и проигравшего один id (ошибки данных); два join-а исходника по (игрок, матч) превратили каждый из трёх со статистикой в 8 строк вместо 2. Тестовой части (2022+) это не касается: 121 528 строк в обоих подсчётах.

---

## Карта документов

| Файл | Что внутри |
|---|---|
| `docs/AUDIT_Tennis_Live_Next_Game_v1.md` | Аудит концепции, 26 разделов. Баннер сверху со сводкой правок |
| `docs/EXPERIMENT_B_elo_prior_and_process.md` | Эксперименты с числами, errata, три ревью, измерения живого рынка |
| `docs/Tennis_Next_Game_Audit_v1.pdf` | Аудит для чтения с телефона (первая редакция) |
| `docs/Tennis_Audit_Bundle_for_review.md` | Самодостаточный пакет для внешнего ревью |
| `docs/TRACK_A_betboom_capture.md` | Как записывать гейм-рынки BetBoom: протокол, восстановленная схема, что проверено и что нет |
| `START_HERE.md` | **Вход с нуля.** Что за проект, все числа, GitHub, установка, карта репозитория, грабли, риски |
| `requirements.txt` | Зависимости по группам: движок, коллектор, исследование |
| `docs/MARKET_MARGINS_2026-09-22.md` | Что контора котирует и сколько это стоит: маржа по рынкам, следующий гейм, требуемое преимущество |
| [Дашборд «Следующий гейм»](https://claude.ai/artifact/GUxjgFDRg8KYcoRHD4kV43) | Обзор проекта на одной странице. **Не файл репозитория** — приватный артефакт |
| `tennis/README.md` | Код сборки: коллектор и движок, статус каждого модуля |
| `tennis/markov/README.md` | Вероятностное ядро: что экспонирует, откуда взято, какие допущения и зачем существуют тесты |
| `research/` | Весь код и результаты прогонов, см. `research/README.md` |

**Что читать в аудите:** раздел 3 (где концепция ошибается), раздел 12 (рынок), раздел 26 (что строить первым), приложение C (расчёты). Раздел 10 про видео устарел, ветка закрыта. Раздел 5 про литературу — числа непроверенные.

---

## Как поднять новую сессию

С 23.09 у каждой сессии своя роль и свои папки: роли и промпт для каждой —
`docs/SESSIONS.md`. Общий вид промпта:

```
Репозиторий geogrgik-debug/mily, ветка claude/tennis-prediction-audit-kskn26.
Прочитай START_HERE.md, потом HANDOFF.md.
Работаем над <задача от владельца>.
Перед работой сделай git pull: в эту ветку пушат несколько сессий.
Общие документы (START_HERE, HANDOFF, tennis/README.md, доску) не правь —
раздел 0 START_HERE; свой итог пиши в сообщение коммита.
```

В эту ветку пушили параллельно из разных сессий, и это уже приводило к отказу на
пуш с разведением конфликтов руками. `git pull` перед началом — не формальность.
А общие документы правит одна сессия-координатор (раздел 0 `START_HERE.md`):
когда их правили все, они разъезжались с кодом — 23.09 готовый сборщик строк
полдня значился «следующим».

Отдельно: автоматический сбор почти наверняка противоречит пользовательскому соглашению конторы. Это решение владельца проекта, не техническое ограничение.

Три внешних ревью уже проведены (Gemini, Fable, Astra). Их находки разобраны в разделах документа с экспериментами. Четвёртое ревью того же материала смысла не имеет.
